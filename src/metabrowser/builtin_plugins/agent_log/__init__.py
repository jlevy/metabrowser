"""Built-in agent-log plugin: Claude / Gemini / Pi JSONL session logs.

Present for the same reason as the binary plugin's marker: it keeps
``sidekick`` uniquely named under pytest's ``src`` collection.
"""
